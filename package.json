{
  "name": "plusduel",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "shared",
    "client"
  ],
  "scripts": {
    "dev:client": "npm run dev --workspace client",
    "build": "npm run build --workspace shared --if-present && npm run build --workspace client",
    "build:client": "npm run build --workspace client",
    "typecheck": "npm run typecheck --workspaces --if-present && npm run typecheck:functions",
    "typecheck:functions": "tsc --noEmit -p netlify/functions/tsconfig.json",
    "test": "npm run test --workspace shared"
  },
  "dependencies": {
    "@netlify/blobs": "^11.0.3"
  }
}
